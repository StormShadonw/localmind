import 'dart:io';

import 'package:localmind/models/model.dart';
import 'package:tflite_flutter/tflite_flutter.dart';

class TensorFlowHelper {
  //   Future<void> sendResponse(Model model, String message) async {

  //     final interpreter = await Interpreter.fromFile(File(model.downloadedPath ?? ""));
  // var input = message;  // Ajusta según tu modelo
  // var output = List.filled(outputSize, 0).reshape(outputShape);
  // interpreter.run(input, output);
  //   }

  //   final interpreter = await Interpreter.fromAsset('assets/model.tflite');
  // var input = [...];  // Ajusta según tu modelo
  // var output = List.filled(outputSize, 0).reshape(outputShape);
  // interpreter.run(input, output);
}
